<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 #{nombrePagina}

            if error
              p.alerta.error= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="usuario") Usuario:
                    input(type="text" name="usuario" placeholder="Tu Usuario" )
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu Password" )
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Sesión")
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aún no tienes cuenta? Crea una gratis
                    a(href="/cuenta-local") Inicia Sesión con Cuenta Local